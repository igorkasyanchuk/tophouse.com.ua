.center_block
  %h2= t('sign_up')
  - semantic_form_for @user do |form|
    = form.error_messages
    = render :partial => "/users/inputs", :locals => { :form => form }
    - form.buttons do
      = form.commit_button t('sign_up_button')
