%h1.heading
  = "Editing region %s" % @region.id
  
- semantic_form_for(@region) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', collection_path